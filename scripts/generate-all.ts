import fs from "node:fs";import path from "node:path";import {publicAlbums,allMedia,years} from "../lib/content";const root=process.cwd(),url=process.env.NEXT_PUBLIC_SITE_URL||'https://festivals-rvp.pages.dev';const albums=publicAlbums(), media=allMedia();const routes=['','years','gallery','timeline','festivals','trips','videos','documents','favorites','search','about',...years().map(y=>`years/${y}`),...albums.map(a=>`years/${a.year}/${a.category.toLowerCase()}/${a.slug}`)];fs.writeFileSync(path.join(root,'public','search-index.json'),JSON.stringify(media.map(m=>({title:m.title,date:m.date,tags:m.tags,url:`/years/${m.album.year}/${m.album.category.toLowerCase()}/${m.album.slug}/`})),null,2));fs.writeFileSync(path.join(root,'public','sitemap.xml'),`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${routes.map(r=>`<url><loc>${url}/${r}</loc></url>`).join('')}</urlset>`);fs.writeFileSync(path.join(root,'public','feed.xml'),`<?xml version="1.0"?><rss version="2.0"><channel><title>RVP Memories</title><link>${url}</link><description>Family memories</description>${albums.map(a=>`<item><title>${a.title}</title><link>${url}/years/${a.year}/${a.category.toLowerCase()}/${a.slug}/</link><description>${a.description}</description></item>`).join('')}</channel></rss>`);console.log(`Generated sitemap, search index and RSS for ${albums.length} albums.`);
