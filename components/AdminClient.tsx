"use client"; import {useState} from "react"; export function AdminClient(){const [message,setMessage]=useState("");async function action(kind:string){const r=await fetch(`/api/admin/${kind}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({})});setMessage(r.ok?"Saved. Refresh to see updates.":"Unable to save: sign in or check the admin server.")}return <section className="adminbox"><h2>Archive controls</h2><p>Use the local admin server to safely manage the archive. Public visitors never receive edit controls.</p><div className="admin-actions">{["create","upload","ingest","publish"].map(x=><button key={x} onClick={()=>action(x)}>{x[0].toUpperCase()+x.slice(1)}</button>)}</div>{message&&<p className="notice">{message}</p>}<p><code>npm run ingest</code> processes ZIPs from inbox; <code>npm run publish</code> commits and pushes approved changes.</p></section>}
