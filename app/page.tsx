import Link from "next/link"; import {publicAlbums,years} from "@/lib/content"; import {PrivateNotice} from "@/components/PrivateNotice"; export default function Home(){const albums=publicAlbums();return <main><section className="hero"><p className="eyebrow">Govardhan Reddy&apos;s family archive</p><h1>Stories worth<br/>remembering.</h1><p>Festivals, journeys and the everyday magic in between.</p><p><Link className="more" href="/gallery/">Explore the memories</Link></p></section><PrivateNotice/><h2>Recent chapters</h2><div className="grid">{albums.slice(0,6).map(a=><Link className="card" href={`/years/${a.year}/${a.category.toLowerCase()}/${a.slug}/`} key={a.slug}><p className="eyebrow">{a.year} · {a.category}</p><h3>{a.title}</h3><p>{a.media.length} memories</p></Link>)}</div><h2>Browse by year</h2><div className="grid">{years().map(y=><Link className="card" href={`/years/${y}/`} key={y}><h3>{y}</h3><p>Open this chapter →</p></Link>)}</div></main>}
